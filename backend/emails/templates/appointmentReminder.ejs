<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointment Reminder</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #2c5aa0;
            margin-bottom: 10px;
        }
        .title {
            color: #2c5aa0;
            font-size: 24px;
            margin-bottom: 20px;
            text-align: center;
        }
        .content {
            margin-bottom: 30px;
        }
        .appointment-card {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #2c5aa0;
            margin: 20px 0;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .detail-row:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: bold;
            color: #495057;
        }
        .detail-value {
            color: #212529;
        }
        .reminder-info {
            background-color: #e8f4fd;
            padding: 15px;
            border-radius: 5px;
            border-left: 4px solid #2c5aa0;
            margin: 20px 0;
            text-align: center;
        }
        .actions {
            text-align: center;
            margin: 30px 0;
        }
        .button {
            display: inline-block;
            padding: 12px 24px;
            margin: 5px 10px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
        }
        .button-primary {
            background-color: #2c5aa0;
            color: white;
        }
        .button-secondary {
            background-color: #6c757d;
            color: white;
        }
        .important-info {
            background-color: #fff3cd;
            padding: 15px;
            border-radius: 5px;
            border-left: 4px solid #ffc107;
            margin: 20px 0;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            color: #666;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">HealthCare Platform</div>
        </div>
        
        <h1 class="title">📅 Appointment Reminder</h1>
        
        <div class="content">
            <p>Dear <%= recipientName %>,</p>
            
            <% if (recipientType === 'patient') { %>
                <p>This is a friendly reminder about your upcoming appointment with Dr. <%= doctorName %>.</p>
            <% } else { %>
                <p>This is a reminder about your upcoming appointment with patient <%= patientName %>.</p>
            <% } %>
            
            <div class="reminder-info">
                <h3 style="margin: 0; color: #2c5aa0;">Your appointment is <%= reminderTime %></h3>
            </div>
            
            <div class="appointment-card">
                <div class="appointment-details">
                    <div class="detail-row">
                        <span class="detail-label">Appointment ID:</span>
                        <span class="detail-value">#<%= appointmentId %></span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Date & Time:</span>
                        <span class="detail-value"><%= appointmentDate %> at <%= appointmentTime %></span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Duration:</span>
                        <span class="detail-value"><%= duration %> minutes</span>
                    </div>
                    <% if (recipientType === 'patient') { %>
                        <div class="detail-row">
                            <span class="detail-label">Doctor:</span>
                            <span class="detail-value">Dr. <%= doctorName %></span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Specialization:</span>
                            <span class="detail-value"><%= doctorSpecialization %></span>
                        </div>
                    <% } else { %>
                        <div class="detail-row">
                            <span class="detail-label">Patient:</span>
                            <span class="detail-value"><%= patientName %></span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Patient Contact:</span>
                            <span class="detail-value"><%= patientPhone %></span>
                        </div>
                    <% } %>
                    <% if (appointmentType) { %>
                        <div class="detail-row">
                            <span class="detail-label">Type:</span>
                            <span class="detail-value"><%= appointmentType %></span>
                        </div>
                    <% } %>
                    <% if (location) { %>
                        <div class="detail-row">
                            <span class="detail-label">Location:</span>
                            <span class="detail-value"><%= location %></span>
                        </div>
                    <% } %>
                </div>
            </div>
            
            <% if (recipientType === 'patient') { %>
                <div class="important-info">
                    <strong>Preparation Checklist:</strong><br>
                    • Arrive 15 minutes early for check-in<br>
                    • Bring a valid ID and insurance card<br>
                    • Prepare any questions you want to ask<br>
                    • Bring current medications list<br>
                    • Have any relevant medical records ready
                </div>
            <% } else { %>
                <div class="important-info">
                    <strong>Preparation Reminders:</strong><br>
                    • Review patient's medical history<br>
                    • Prepare consultation materials<br>
                    • Ensure equipment is ready<br>
                    • Check for any pre-appointment notes<br>
                    • Verify appointment details
                </div>
            <% } %>
            
            <div class="actions">
                <% if (dashboardUrl) { %>
                    <a href="<%= dashboardUrl %>" class="button button-primary">View Dashboard</a>
                <% } %>
                <% if (rescheduleUrl) { %>
                    <a href="<%= rescheduleUrl %>" class="button button-secondary">Reschedule</a>
                <% } %>
            </div>
            
            <p>If you need to reschedule or have any questions, please contact us as soon as possible.</p>
            
            <p>We look forward to seeing you at your appointment!</p>
            
            <p>Best regards,<br>
            The Healthcare Platform Team</p>
        </div>
        
        <div class="footer">
            <p>This is an automated reminder. Please save this email for your records.</p>
            <p>Need help? Contact us at support@healthcareplatform.com or call (555) 123-4567</p>
            <p>&copy; 2024 Healthcare Platform. All rights reserved.</p>
        </div>
    </div>
</body>
</html>